import 'package:flutter/material.dart';

class JsonPreviewPage extends StatelessWidget {
  const JsonPreviewPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
