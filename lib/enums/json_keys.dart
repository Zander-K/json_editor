enum JsonKeys {
  title,
  description,
  buttonText,
  buttonNavigation,
  closeButtonNavigation,
  titleColor,
  buttonColor,
  modalBackgroundColor,
  bodyPadding,
  buttonPadding,
  isError,
  redirectURL,
}
